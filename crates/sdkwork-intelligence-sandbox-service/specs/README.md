# Sandbox Service Component Contract

`component.spec.json` is the machine authority for the L2 Sandbox lifecycle service. It declares the Sandbox-qualified Session lifecycle and repository ports plus the required Provider SPI under `REQ-2026-0002`.
