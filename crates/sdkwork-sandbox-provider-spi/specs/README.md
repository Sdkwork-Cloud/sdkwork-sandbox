# Provider SPI Component Contract

`component.spec.json` is the machine authority for this crate. It links to global SDKWork standards by relative path and declares the `0.1` candidate `sandboxProvider` port governed by `REQ-2026-0002` and its proposed lifecycle ADR.
