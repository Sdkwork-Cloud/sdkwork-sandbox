# Local Provider Component Contract

`component.spec.json` is the component integration authority for the future Local Provider. The cross-component Host boundary is defined by `../../../specs/sandbox-local-provider-host-boundary.contract.json`. Both remain pre-implementation evidence: the adapter deliberately has no public or required port until the Requirement, ADR, dependency set, platform supervision and real-runner security evidence are accepted.
