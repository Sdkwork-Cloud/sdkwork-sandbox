# Local Provider Component Contract

`component.spec.json` is the machine authority for the future local provider. The adapter deliberately has no public or required port until isolation policy is accepted.
