# Repository Agent Plugins

Purpose: installable agent-tool plugins owned by this repository, distinct from runtime/provider source under root `plugins/`.

Each installable plugin must contain `.codex-plugin/plugin.json`, document permissions and external services, and contain no secrets. No repository agent plugin is installed in Phase 0.
