# Repository Skills

Purpose: reusable, repository-owned agent workflows for SDKWork Sandbox.

Add a skill only when repeated work needs a stable workflow. Each skill uses a lowercase kebab-case directory with `SKILL.md`, cites the applicable root specs, documents inputs and verification, and contains no credentials or machine-specific paths.

No repository skill is installed in Phase 0.
